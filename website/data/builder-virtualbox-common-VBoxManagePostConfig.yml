source_path: builder/virtualbox/common/vboxmanage_post_config.go
name: VBoxManagePostConfig
required: []
not_required:
- name: vboxmanage_post
  type: '[][]string'
  docs: |
    Identical to vboxmanage,
    except that it is run after the virtual machine is shutdown, and before the
    virtual machine is exported.
